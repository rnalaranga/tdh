<%- include('partials/header', { page: 'gallery', title: 'Project Gallery — TD HANDYMAN', description: 'View our recent property transformations across South East Melbourne.' }) %>

<!-- ─── PAGE HERO ────────────────────────────────────────────────────────── -->
<section class="page-hero">
  <div class="page-hero-bg">
    <img src="/images/service_rendering.png" alt="TD HANDYMAN Recent Projects" />
  </div>
  <div class="container page-hero-content reveal">
    <span class="eyebrow">Our Work</span>
    <h1 class="section-title">Project <em>Gallery</em></h1>
  </div>
</section>

<!-- ─── GALLERY SECTION ──────────────────────────────────────────────────── -->
<section class="section" style="padding-top: 0;">
  <div class="container">
    
    <!-- Filter Buttons -->
    <div class="gallery-filters reveal">
      <button class="filter-btn active" data-filter="all">All Projects</button>
      <button class="filter-btn" data-filter="landscaping">Landscaping</button>
      <button class="filter-btn" data-filter="retaining-walls">Retaining Walls</button>
      <button class="filter-btn" data-filter="rendering">Rendering</button>
      <button class="filter-btn" data-filter="tiling">Tiling</button>
    </div>

    <!-- Gallery Grid (Masonry Style) -->
    <div class="gallery-masonry" id="lightgallery">
      
      <!-- Item 1 -->
      <a href="/images/service_landscaping.png" class="gallery-item reveal landscaping">
        <img src="/images/service_landscaping.png" alt="Landscape Transformation" loading="lazy" />
      </a>

      <!-- Item 2 -->
      <a href="/images/service_retaining_walls.png" class="gallery-item reveal retaining-walls" style="transition-delay: 100ms;">
        <img src="/images/service_retaining_walls.png" alt="Retaining Wall Construction" loading="lazy" />
      </a>

      <!-- Item 3 -->
      <a href="/images/service_rendering.png" class="gallery-item reveal rendering" style="transition-delay: 200ms;">
        <img src="/images/service_rendering.png" alt="Exterior House Rendering" loading="lazy" />
      </a>

      <!-- Item 4 -->
      <a href="/images/service_tiling.png" class="gallery-item reveal tiling">
        <img src="/images/service_tiling.png" alt="Outdoor Patio Tiling" loading="lazy" />
      </a>

      <!-- Item 5 -->
      <a href="/images/service_painting.png" class="gallery-item reveal rendering" style="transition-delay: 100ms;">
        <img src="/images/service_painting.png" alt="Interior Painting Project" loading="lazy" />
      </a>

      <!-- Item 6 -->
      <a href="/images/service_wall_moulding.png" class="gallery-item reveal tiling" style="transition-delay: 200ms;">
        <img src="/images/service_wall_moulding.png" alt="Modern Wall Moulding" loading="lazy" />
      </a>

    </div>
  </div>
</section>

<!-- Simple Gallery Filtering Script -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const items = document.querySelectorAll('.gallery-item');

    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');

        const filterValue = btn.getAttribute('data-filter');

        items.forEach(item => {
          if (filterValue === 'all' || item.classList.contains(filterValue)) {
            item.style.display = 'block';
            setTimeout(() => { item.style.opacity = '1'; item.style.transform = 'scale(1)'; }, 50);
          } else {
            item.style.opacity = '0';
            item.style.transform = 'scale(0.95)';
            setTimeout(() => { item.style.display = 'none'; }, 400);
          }
        });
      });
    });
  });
</script>

<%- include('partials/footer') %>
